<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.quincy.test.mapper.TestMapper" >
	<update id="update">
		UPDATE ${tableName} 
		<set>
			${fieldName} = #{newFieldParam}
		</set>
		<where>
			${fieldName} = #{oldFieldParam}
		</where>
	</update>
	<update id="update2">
		UPDATE b_region 
		<set>
			en_name = #{newFieldParam}
		</set>
		<where>
			en_name = #{oldFieldParam}
		</where>
	</update>
</mapper>